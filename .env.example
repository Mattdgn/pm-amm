# Anchor provider
ANCHOR_PROVIDER_URL=http://localhost:8899
ANCHOR_WALLET=~/.config/solana/id.json

# Solana cluster: "devnet" or "localnet"
NEXT_PUBLIC_SOLANA_CLUSTER=devnet

# Vercel KV / Upstash Redis (optional — sparklines fall back to seed data without it)
KV_REST_API_URL=
KV_REST_API_TOKEN=
